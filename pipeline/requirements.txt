# Pipeline de dados (só os fetchers de API precisam disto; build_graph.py roda com stdlib).
yfinance>=0.2.40      # market cap de ações (grátis, sem chave)
requests>=2.31        # CoinGecko + World Bank
